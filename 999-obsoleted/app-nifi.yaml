apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: nifi
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'nifi-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/otep-dc-k3s.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/nifi'
      destination:
        name: in-cluster
        namespace: nifi
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
