apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: discord-alm
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'discord-alm-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/otep-dc-k3s.git
        targetRevision: "{{name}}" #Based on environment
        path: 'manifests/discord-alm'
      destination:
        name: in-cluster
        namespace: discord-alm
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
