apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: nifi-sso
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'nifi-sso-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/otep-dc-k3s.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/nifi-sso'
      destination:
        name: in-cluster
        namespace: nifi-sso
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
