apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: postgresql-dev
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'postgresql-dev-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/otep-dc-k3s.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/postgresql-dev'
        helm:
          ignoreMissingValueFiles: true
          valueFiles:
            - values.yaml
            - values-{{name}}.yaml
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
