apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nifi-cert-sso
spec:
  secretName: nifi-cert-sso
  dnsNames:
    - {{ .Values.domain }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
