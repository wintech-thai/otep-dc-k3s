apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio-ing
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
spec:
  tls:
    - hosts:
      - {{ .Values.domain }}
      secretName: minio-cert
  rules:
    - host: {{ .Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio-main-console
                port:
                  number: 9090
