fullnameOverride: prometheus

prometheus-node-exporter:
  hostRootFsMount:
    enabled: false

grafana:
  admin:
    # Initially created by ArgoCD
    existingSecret: "grafana-credentials"
    userKey: GRAFANA_USER
    passwordKey: GRAFANA_PASSWORD
  testFramework:
    enabled: false

alertmanager:
  alertmanagerSpec:
    alertmanagerConfigMatcherStrategy:
      type: None
